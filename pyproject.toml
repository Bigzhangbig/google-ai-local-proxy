[project]
name = "google-ai-local-proxy"
version = "2.0.0"
description = "Local proxy: multi-key × multi-model fallback for Google AI"
requires-python = ">=3.10"
dependencies = ["flask>=3.0", "requests>=2.31"]

[project.scripts]
proxy = "server:app"
